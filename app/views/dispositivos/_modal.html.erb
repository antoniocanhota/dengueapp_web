<div id=<%= "modalUpdateApelidoDispositivo#{dispositivo.id}" %> class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby=<%= "modalUpdateApelidoDispositivo#{dispositivo.id}" %> aria-hidden="true">
<%= form_for(dispositivo) do |d| %>
    <div class="modal-header">
      <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
      <h3 id="myModalLabel">Alterar Apelido do Dispositivo #<%= dispositivo.id%></h3>
    </div>
    <div class="modal-body">
        <%= d.text_field :apelido %>
    </div>
    <div class="modal-footer">
      <%=  d.submit "Salvar", :class => "btn btn-primary" %>
    </div>
<% end %>
</div>